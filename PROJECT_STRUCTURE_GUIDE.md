# 프로젝트 운영 지침서 (범용 템플릿)

> 검증된 **협업 모델 · 작업 방식 · 품질 기준**을 모든 프로젝트에 이식하기 위해 일반화한
> 것이다. 이 문서는 **두 층위**로 나뉜다.
>
> - **제1부 · 범용 코어** — 스택과 무관하게 항상 적용되는 원칙. 그대로 가져간다.
> - **제2부 · 스택 프로파일** — 스택별로 갈아끼우는 구체 구현(디렉토리·툴링). 새 스택이면
>   해당 프로파일만 바꾸면 된다.
>
> 원칙은 코어에, "리터럴"(디렉토리 트리·툴 이름)은 프로파일에. 새 프로젝트를 시작할 때
> **코어는 복사, 프로파일은 선택**한다.

---

## Part 0 — 이 지침서 사용법

### 0.1 새 프로젝트에 넣을 3종 세트
1. **`CLAUDE.md`** ← 1장의 **복붙용 블록 A** + 0.2의 **참조 한 줄**
2. **`.claude/agents/worker.md`** ← 1장의 **복붙용 블록 B**
3. **`PROJECT_STRUCTURE_GUIDE.md`** ← 이 파일 자체를 복사

그다음 **제2부에서 스택에 맞는 프로파일 하나를 골라** 구조·테스트 툴링을 그 프로파일대로 잡는다.

### 0.2 자동 적용을 위한 참조 연결 (빠뜨리면 코어가 자동 적용 안 됨)
Claude Code는 세션 시작 시 **`CLAUDE.md`와 `.claude/agents/*.md`만 자동 로드**한다.
이 지침서 파일 자체는 자동으로 읽히지 않으므로, `CLAUDE.md`에 아래 참조 한 줄을 넣어야
제1부·제2부까지 자동 적용된다.

```markdown
## 프로젝트 운영 지침
작업 방식·구조·품질 기준은 `PROJECT_STRUCTURE_GUIDE.md`를 따른다.
제1부(범용 코어) 전체 + 제2부에서 이 프로젝트 스택에 해당하는 프로파일을 적용하라.
새 작업 시작 전 이 파일을 먼저 읽고 그 원칙에 맞춰 진행하라.
```

정리:
- **블록 A + 블록 B** → 협업 모델은 자동 로드되어 바로 동작.
- **참조 한 줄** → 지침서 본문(코어 + 프로파일)까지 끌어오는 고리.
- 이 연결이 없으면 협업 모델만 돌고 나머지는 매번 수동으로 짚어줘야 한다.

### 0.3 원칙 분리
이 파일은 **메타 지침(모든 프로젝트 공통)**, 각 프로젝트의 `CLAUDE.md`는 **그 프로젝트
고유 지침**이다. 둘을 섞지 않는다. 단, 0.2의 참조 한 줄만 예외적으로 `CLAUDE.md`에 두어
메타 지침을 끌어오는 다리 역할을 한다.

### 0.4 자동화의 한계
설정이 갖춰져도 보장되는 것은 **워크플로 뼈대**(위임 구조·검증 습관·폴더 규칙)까지다.
심층 분석의 깊이는 매 세션의 요청 방식에 달려 있다. 지침서는 "어떻게 일할지"의 틀을
고정할 뿐, 매 답변의 밀도를 자동 복제하지는 않는다.

---
---

# 제1부 · 범용 코어 (모든 프로젝트 공통)

## 1. AI 협업 모델: Advisor / Worker

### 1.1 핵심 개념
작업을 **판단(Advisor)**과 **구현 노동(Worker)**으로 분리한다. 메인 세션(상위 모델)은
Advisor로서 판단에 집중하고, 실제 코드 작성은 Worker(하위 모델 서브에이전트)에게 위임한다.
상위 모델의 컨텍스트를 판단·설계·검증에 아끼고, 반복 구현은 저렴한 모델로 병렬 처리한다.

### 1.2 Advisor(메인 세션)가 직접 하는 일
- 요구사항 분석, 작업 분해, 설계 결정
- Worker에게 줄 작업 브리프 작성
- 결과 검증: diff 직접 확인, 테스트 직접 실행
- 최종 커밋 승인, 사용자 보고

### 1.3 Worker(서브에이전트)에게 위임하는 일
- 코드 작성과 수정, 테스트 작성 등 구현 작업 전부
- Agent 도구로 위임하고 model은 하위 모델(예: `sonnet`)을 지정한다
- 서로 독립적인 작업은 병렬로 위임한다

### 1.4 브리프 기준
Worker가 컨텍스트를 재탐색하지 않도록 Advisor가 이미 파악한 내용을 담는다.
- 관련 **파일 경로** / 지켜야 할 **프로젝트 컨벤션** / 알려진 **함정** / **완료 기준**(통과할 테스트·빌드)

### 1.5 경계
- Worker의 완료 보고를 그대로 믿지 않는다. **diff와 테스트로 직접 확인한 뒤 승인**한다.
- 검증 실패는 수정 브리프로 **재위임**한다. 직접 수정은 사소한 마무리에만.
- 위임 오버헤드가 결과물보다 큰 작업(한두 줄)은 Advisor가 직접 처리.
- **판단·설계·문서 합성처럼 컨텍스트가 핵심인 작업은 위임하지 않는다.**

### 1.6 이중 검증 구조
- **Worker 셀프 검증**: 완료 기준 테스트/빌드를 직접 실행해 통과 확인 후 보고. 실행 없이 통과 주장 금지.
- **Advisor 최종 검증**: diff + 테스트 재확인 + 가능하면 실동작 구동(4.4 DoD)까지 본 뒤 커밋 승인.

### 복붙용 블록 A — 새 프로젝트 `CLAUDE.md`
```markdown
## 모델 역할 분담: Advisor / Worker

너는 Advisor다. 판단에 집중하고, 구현 노동은 Worker에게 위임하라.

Advisor(너, 메인 세션)가 직접 하는 일:
- 요구사항 분석, 작업 분해, 설계 결정
- Worker에게 줄 작업 브리프 작성
- 결과 검증: diff 직접 확인, 테스트 직접 실행
- 최종 커밋 승인, 사용자 보고

Worker(서브에이전트)에게 위임하는 일:
- 코드 작성과 수정, 테스트 작성 등 구현 작업 전부
- Agent 도구로 위임하고 model은 하위 모델(예: "sonnet")을 지정한다
- 서로 독립적인 작업은 병렬로 위임한다

브리프 기준:
- 네가 이미 파악한 컨텍스트를 담아 Worker가 재탐색하지 않게 하라
- 파일 경로, 프로젝트 컨벤션, 알려진 함정, 완료 기준(통과해야 할 테스트)을 포함하라

경계:
- Worker의 완료 보고를 그대로 믿지 마라. diff와 테스트로 직접 확인한 뒤 승인하라
- 검증 실패는 수정 브리프로 재위임하라. 직접 수정은 사소한 마무리에만 허용된다
- 한두 줄 수정처럼 위임 오버헤드가 더 큰 작업은 직접 처리해도 된다
- 판단·설계·문서 합성처럼 컨텍스트가 핵심인 작업은 위임하지 말고 직접 처리하라
```

### 복붙용 블록 B — 새 프로젝트 `.claude/agents/worker.md`
```markdown
---
name: worker
description: Advisor가 위임하는 구현 작업 전담 Worker. 코드/테스트 작성, 리팩터링 등 실제 구현 노동을 수행한다. 판단이나 요구사항 분해가 아니라 이미 분해된 작업 브리프를 받아 실행하는 용도로 사용한다.
model: sonnet
---

너는 Worker다. Advisor(메인 세션)가 분해한 작업 브리프를 받아 구현을 실행한다.

## 역할
- 브리프에 명시된 범위만 구현한다. 브리프에 없는 리팩터링, 추상화, 클린업은 하지 않는다.
- 브리프에 파일 경로, 컨벤션, 함정, 완료 기준(테스트)이 포함되어 있다면 그대로 따른다. 빠진 정보가 있으면 코드베이스를 직접 탐색해 채운다.
- 완료 기준으로 명시된 테스트/타입체크/빌드를 직접 실행해 통과를 확인한 뒤 보고한다. 실행하지 않고 통과를 주장하지 않는다.
- 프로젝트에 로컬 레퍼런스 문서(벤더 문서, 사내 규약 등)가 있으면, 관례나 API가 익숙한 것과 다를 때 코드 작성 전 해당 문서를 확인한다.

## 보고
- 무엇을 바꿨는지(diff 관점), 어떤 검증을 실행했고 결과가 어땠는지 간결히 보고한다.
- 브리프 범위를 벗어나 발견한 이슈는 "발견 사항"으로 별도 표시하고, 임의로 수정하지 않는다.
```

---

## 2. Git / 커밋 컨벤션

- **커밋 단위 = 하나의 논리적 변경.** 서로 다른 관심사를 한 커밋에 섞지 않는다.
- **메시지 포맷**: 제목은 `type: 요약`(Conventional Commits — `feat/fix/refactor/docs/chore/test`).
  본문에는 **무엇을 바꿨나가 아니라 "왜"**를 쓴다. 어떤 문제가 있었고 무엇으로 고쳤는지.
- **커밋 시점**: 사용자가 요청할 때만 커밋/푸시한다. 기본 브랜치면 먼저 브랜치를 판다.
- **검증 통과 후 커밋**: 테스트가 깨진 채로 커밋하지 않는다(1.6 이중 검증 통과 후).
- 협업 흔적이 필요하면 커밋 푸터에 `Co-Authored-By:` 등을 남긴다.

## 3. 프로덕션 리뷰 아티팩트 (심각도 태깅 + 결정 기록)

코드 리뷰·프로덕션 검토 결과를 **재사용 가능한 문서**로 남긴다. 형식:

- 맨 위 **총평** — 핵심 불변식이 지켜지는지 한 문단.
- **심각도 태깅** — `Blocker`(핵심 불변식 파괴) / `Major`(신뢰성·보안) / `Minor`(개선).
- **상태 구분** — `✅ 해결됨`(근거 커밋 해시 포함) / `🟠 보류`(고치지 않기로 한 **이유와 조건**
  명시 — 예: "로컬 데모 전용이라 의도적 보류, 실배포 시 필수") / `🟡 Minor`.
- **심층 항목은 PAR 문서로 분리** — `Problem → Analysis → Action → Result` 구조, 근거 커밋
  해시 포함. "됐다/안됐다"가 아니라 **왜 문제였고 무엇으로 고쳤는지**를 기록해야 회고·포트폴리오
  가치가 생긴다.
- 검토 날짜와 갱신 이력을 남겨 시점을 추적한다.

> 이 프로젝트의 `docs/PRODUCTION_READINESS.md`(요약·심각도)와 `docs/analysis/*.md`(PAR
> 심층)의 2단 구성이 표준 예시다. 문서 간 역할 경계는 `docs/README.md`(문서 지도)로 관리한다.

## 4. 테스트 원칙 (스택 무관 — 툴링은 프로파일 참조)

### 4.1 테스트 레벨
- **단위** — 순수 로직. 빠르고 많이.
- **통합·동시성** — 실제 의존성(DB·브로커) 대상. **동시성/정합성 버그는 목(mock)으로 재현되지
  않는다.** 반드시 실 의존성으로 검증한다.
- **E2E** — 요청 유입부터 상태 확정까지 실제 경로.

### 4.2 정확한 불변식을 단언한다
"깨지지만 않으면 OK"(느슨한 경계)가 아니라 **정확히 이 값**을 단언한다.
- 나쁨: `assertThat(success).isLessThanOrEqualTo(5)`, `balance >= 0`
- 좋음: `assertThat(success).isEqualTo(5)` + `assertThat(rejected).isEqualTo(5)` + `balance == 0`
- 이유: 느슨한 경계는 "우연히 통과"를 잡아내지 못한다. 정확한 값이라야 회귀를 잡는다.

### 4.3 완료 기준으로서의 테스트
통과해야 할 테스트를 Worker 브리프의 **완료 기준**으로 명시한다(1.4). 완료 보고는 그 테스트
실행 결과로 증명한다(1.6).

### 4.4 Definition of Done — 실동작 확인
"테스트 통과"가 끝이 아니다. 런타임 표면이 있는 변경은 **실제 플로우를 end-to-end로 구동해
눈으로 동작을 확인**한 뒤 완료로 본다(Advisor 최종 검증에 포함).

## 5. 코딩 원칙 (스택 무관)

```
가독성
- 메서드는 한 가지 일만 (조회+검증+저장 동시 금지, 길면 분리)
- 이름은 동작·의미를 그대로 설명
- 매직 넘버는 상수로, 조건문은 의미 있는 이름의 메서드로 (validateX, isY)

복잡도
- 인터페이스는 구현체 2개 이상 또는 테스트 대역 필요 시에만 (XxxImpl 남발 금지)
- 커스텀 예외로 의미 부여 (예: SoldOutException)
- 어노테이션/매크로 사용 범위를 명시 제한 (예: @Getter만, @Data/@Setter 금지)

동시성·정합성  (원리는 스택 무관, 구현은 프로파일)
- check-then-act 배제, 조건부 UPDATE 한 문장으로 확인+실행 원자화
  (SQL:  UPDATE ... SET x = x - :n WHERE id = :id AND x >= :n)
- 부분 실패는 커밋하지 않는다 — 실패 시 예외로 전체 롤백해 재처리 루프에 다시 태운다
- 재시도/보상 트랜잭션은 멱등하게 (상태 + 시도번호 이중 fencing 등)

로그
- 상태 전이/락 등 핵심 이벤트는 INFO로 남겨 이후 분석에 활용

구조 (원리 — 인스턴스화는 프로파일)
- 진입점(컨트롤러/route/action)은 얇게, 비즈니스 로직은 별도 계층으로
- 예외는 전역 핸들러에서 통일 처리
- 위험한 운영 동작(초기화 등)은 환경/프로파일로 분리
- 공통 관심사(설정·예외·유틸)는 한 곳에 모은다

코드 작성 단위 (AI 협업 시 특히 중요 — 1장과 연결)
- 한 번에 하나의 파일
- 논리적 단계가 2개 이상이면 나눠 진행하고 각 단계 후 확인
- 이 단위를 Advisor가 Worker에게 주는 브리프 분해 단위와 일치시킨다
```

## 6. 프로젝트 `CLAUDE.md`에 넣을 섹션 구성
0. **모델 역할 분담: Advisor / Worker** — 블록 A. (맨 위)
1. **프로젝트 개요** — 목표·타겟·핵심 어필 포인트("왜 이 기술을 선택했나"까지)
2. **기술 스택 표**
3. **프로젝트 구조** — 트리 형태, 실제 디렉토리와 동기화(→ 해당 스택 프로파일 따름)
4. **버전별/전략별 구현 설명** — 각 버전이 무엇을 다르게 하는지 1~3줄
5. **함정 & 수정 이력 표** — `#번호 | 영역 | 함정 | 파일 | 수정 방향` 누적
6. **Phase 완료 체크리스트** — `[x]`/`[ ]`로 진행 추적
7. **코딩 컨벤션 & 규칙** — 5장 참고

## 7. 문서 지도 & 설정 원칙
- **문서 지도**: 프로젝트에 문서가 여럿이면 "어떤 문서가 무엇을 담는가"를 한 줄씩 매핑해
  중복·혼선을 막는다. 한 사실은 한 문서에만 두고 나머지는 링크로 참조. 상태(리뷰) ↔ 심층
  (PAR) ↔ 설계(결정) ↔ 학습을 분리한다. 표준 예시는 이 프로젝트의 `docs/README.md`.
- **설정 단일 진실원천**: 서로 연동돼야 하는 값(파티션 수 ↔ 동시성 ↔ 풀 크기 등)은 한 곳에서
  파생시킨다. 여러 곳에 하드코딩하지 않는다.
- **시크릿 분리**: 비밀번호·키는 환경변수/시크릿으로. 커밋에 하드코딩 금지(로컬 데모 예외는
  리뷰 문서에 보류로 명시).

---
---

# 제2부 · 스택 프로파일 (스택별로 갈아끼움)

> 제1부의 **원리를 특정 스택으로 인스턴스화**한 것. 새 스택이면 해당 프로파일만 바꾸고
> 코어는 그대로 둔다. 아래 대응표가 핵심 — 같은 원리가 스택마다 다른 형태로 구현된다.

| 코어 원리 | Spring Boot | Next.js + Supabase |
|---|---|---|
| 진입점은 얇게 | Controller | route handler / server action |
| 기능마다 계층 반복 | `{domain}/` 패키지 | `features/{x}/` 폴더 |
| 공통 관심사 한 곳 | `global/` | `lib/` · `shared/` |
| 조건부 UPDATE 원자화 | JPA `@Modifying` UPDATE | Postgres `UPDATE ... WHERE` / RPC / RLS |
| 통합·동시성 실 의존성 테스트 | MySQL Testcontainers · 실 Kafka | 로컬 Supabase · Playwright |

---

## Profile A — Spring Boot 백엔드

### A.1 최상위 구조
```
{project-root}/
├── src/main/java/{base.package}/
│   ├── {domain-A}/ {domain-B}/     도메인별 패키지
│   ├── global/                     공통 관심사 (config, exception, lock)
│   └── dashboard/                  (선택) 지표 시각화
├── src/main/resources/
│   ├── templates/{domain}/         Thymeleaf 뷰
│   └── static/js/
├── load-test/                      부하/성능 시나리오
├── docs/knowledge/                 학습 정리
├── .claude/{agents/worker.md, CLAUDE.md}
```
최상위는 **코드(src) / 성능검증(load-test) / 지식(docs)**를 분리한다.

### A.2 도메인별 패키지 (계층을 도메인 내부에 반복)
```
{domain}/
├── controller/   라우팅 + 요청/응답 매핑만 (로직 없음)
├── domain/       엔티티
├── dto/          요청/응답 DTO
├── repository/   데이터 접근
└── service/      비즈니스 로직
```
레이어 이름·순서를 전체에서 통일 → 새 도메인은 복사-개명으로 추가.

### A.3 여러 전략 비교 — 버전 서브패키지
동일 기능을 전략별로 여러 버전 구현해 비교할 때: `service/v1, v2, v3 ...` + 컨트롤러도
`ControllerV1~Vn`으로 분리해 독립 엔드포인트로 노출. 버전 공통 로직은 버전 패키지 밖에.
**성격이 근본적으로 다른 기능은 버전 넘버링에 끼우지 말고 독립 패키지로**(예: 대기열 →
`waitingroom/`).

### A.4 정합성 구현
- 조건부 UPDATE: `@Modifying @Query("UPDATE ... WHERE id=:id AND balance>=:amount")`.
  InnoDB row lock이 경합을 직렬화 → 재시도 루프 불필요.
- 부분 실패 시 `throw`로 `@Transactional` 롤백 → 스케줄러 scan이 재처리.

### A.5 테스트 툴링
- 동시성: **MySQL Testcontainers** + 멀티스레드로 실 DB 경합 재현, 정확한 불변식 단언.
- E2E: **실 Kafka**(Testcontainers) 대상 발행~소비 경로, DLT 격리 테스트 포함.
- 프로파일로 파티션 수 등 축소해 결정적 검증(1파티션 블로킹 등) 확보.

---

## Profile B — Next.js + Supabase

### B.1 최상위 구조
```
{project-root}/
├── app/                         라우팅 (App Router) — page/layout/route.ts
├── features/{x}/                기능 단위 (도메인에 해당)
│   ├── components/              그 기능 전용 UI
│   ├── actions.ts               server action (진입점, 얇게)
│   ├── queries.ts               데이터 접근 (Supabase 호출)
│   └── schema.ts                zod 등 입력 검증 스키마
├── components/ui/               공용 프리미티브 (버튼 등)
├── lib/                         공통 관심사 (supabase client, 유틸, 예외) ← global/ 대응
├── supabase/
│   ├── migrations/              스키마 마이그레이션 (SQL)
│   └── functions/               Edge Functions / RPC
├── docs/knowledge/
└── .claude/{agents/worker.md, CLAUDE.md}
```
**원리 대응**: `app/route·action`=얇은 진입점(Controller), `features/{x}`=도메인 패키지,
`lib/`=`global/`, `supabase/migrations`=스키마 진실원천.

### B.2 계층 원칙 (코어 5장의 인스턴스화)
- **route handler / server action은 얇게** — 검증(zod)·분기·데이터 접근은 `queries.ts`/서비스
  모듈로. UI 컴포넌트에 DB 로직을 두지 않는다.
- 기능마다 `features/{x}/` 폴더를 같은 모양으로 반복 → 새 기능은 복사-개명.
- 공통 Supabase 클라이언트·에러·유틸은 `lib/`에 모은다.

### B.3 정합성 구현 (코어 "동시성·정합성"의 Supabase 형태)
- **조건부 UPDATE**: `update(...).eq('id', id).gte('balance', n)` 또는 Postgres 함수로
  `UPDATE ... WHERE id=:id AND balance>=:n` — check-then-act 대신 원자화. 클라이언트에서
  읽고-계산하고-쓰지 않는다.
- **다단계 원자성**: 여러 행/테이블을 함께 바꿔야 하면 **RPC(Postgres 함수) 하나의 트랜잭션**
  으로 처리. 실패 시 함수 전체 롤백.
- **권한·불변식은 RLS로 DB에 못박는다** — 클라이언트 신뢰 금지. `service_role` 키는 서버
  전용, 절대 클라이언트 번들에 넣지 않는다.

### B.4 테스트 툴링
- **로컬 Supabase**(`supabase start`)로 실 Postgres 대상 통합 테스트. 동시성/정합성은 여기서
  실 DB로 재현(목 금지 — 코어 4.1).
- **RLS 정책 테스트** — 권한별로 접근이 정확히 허용/차단되는지 단언(정확한 불변식 — 코어 4.2).
- **E2E: Playwright**로 요청~상태확정 실경로.
- 단위: Vitest 등으로 순수 로직.

### B.5 설정·시크릿 (코어 7장)
- `.env.local`에 키. `NEXT_PUBLIC_*`만 클라이언트 노출, `service_role`은 서버 전용.
- 스키마 변경은 항상 `supabase/migrations`의 SQL로 — 대시보드 수동 변경을 진실원천으로 삼지
  않는다(Spring의 "마이그레이션 도구 전환"과 같은 원칙).

---
