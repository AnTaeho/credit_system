# 프로젝트 구조 가이드 (재사용 템플릿)

> 이 문서는 `ticketing-system` 프로젝트에서 검증된 파일/패키지 구조 원칙을 다른 프로젝트에 이식하기 위해 일반화한 것이다.
> 도메인 이름, 버전 개수 등은 프로젝트마다 다르므로 그대로 복사하지 말고 **원칙만 가져와서** 새 프로젝트 구조에 맞게 적용한다.

---

## 1. 최상위 구조

```
{project-root}/
├── src/main/java/{base.package}/
│   ├── {domain-A}/
│   ├── {domain-B}/
│   ├── global/            공통 관심사 (config, exception, lock 등)
│   └── dashboard/          (선택) 결과/지표 시각화용 별도 도메인
├── src/main/resources/
│   ├── templates/{domain}/  Thymeleaf 등 서버사이드 뷰
│   └── static/js/
├── load-test/ 또는 {perf-test-dir}/   부하/성능 테스트 시나리오
├── docs/
│   └── knowledge/          레벨별 학습 정리 문서
└── CLAUDE.md 또는 AGENTS.md   프로젝트 전체 지침서
```

**원칙**: 최상위는 "코드(src) / 성능검증(load-test) / 지식(docs)"를 명확히 분리한다. 학습·회고 목적의 문서를 코드와 같은 레벨에 두지 않고 `docs/` 하위로 격리한다.

---

## 2. 도메인별 패키지 구조 (계층은 도메인 내부에 반복)

도메인마다 아래 레이어를 동일한 패턴으로 반복한다. 레이어 이름과 순서를 프로젝트 전체에서 통일하면, 새 도메인을 추가할 때 기존 도메인을 그대로 복사해서 이름만 바꾸면 되므로 온보딩 비용이 준다.

```
{domain}/
├── controller/   URL 라우팅 + 요청/응답 매핑만 (비즈니스 로직 없음)
├── domain/       엔티티 (또는 model/)
├── dto/          요청/응답 DTO
├── repository/   데이터 접근
└── service/      비즈니스 로직
```

**원칙**:
- 컨트롤러는 얇게 유지한다 — 검증/분기/트랜잭션은 서비스로.
- 인터페이스는 구현체가 2개 이상이거나 테스트 대역이 필요할 때만 도입한다 (`ServiceImpl` 같은 불필요 레이어 금지).
- 도메인 간 공통으로 쓰이는 것(락, 예외, 설정)은 개별 도메인이 아니라 `global/` 하위에 모은다.

---

## 3. "여러 구현 전략을 비교"해야 할 때 — 버전 서브패키지 패턴

동일한 기능을 서로 다른 전략(락, 알고리즘, 아키텍처)으로 여러 버전 구현하고 성능/트레이드오프를 비교하는 게 목적이라면:

```
service/
├── v1/   (전략 A)
├── v2/   (전략 B)
├── v3/   ...
```

- 컨트롤러도 버전별로 분리(`ControllerV1` ~ `ControllerVn`)해서 각 전략을 독립적인 API 엔드포인트로 노출한다.
- 버전 간 공통 로직(도메인 엔티티, 공통 예외 등)은 버전 서브패키지 밖에 둔다.
- 각 버전의 "함정과 수정 이력"을 표로 남긴다 (아래 6번 참고). 단순히 "됐다/안됐다"가 아니라 **왜 문제였는지, 무엇으로 고쳤는지**를 기록해야 포트폴리오/회고 가치가 생긴다.

**성격이 근본적으로 다른 기능은 버전 넘버링에 억지로 끼워 넣지 않는다.** 예: 단건 요청 직렬화가 목적인 버전들과, 유입 트래픽 자체를 통제(대기열)하는 기능은 설계 목적이 다르므로 `v7`이 아니라 독립 패키지(`waitingroom/` 같은)로 분리한다.

---

## 4. 학습/회고 문서 구조 (`docs/knowledge/`)

```
docs/knowledge/
├── level-01-{topic}.md
├── level-02-{topic}.md
├── ...
```

- 레벨(숫자) + 주제 슬러그로 파일명을 통일해 정렬 순서 = 학습 순서가 되게 한다.
- 인터뷰 대비 Q&A, 프레임워크 특이사항(버전업 breaking change) 등도 같은 디렉토리에 레벨로 편입한다.
- 성능 비교/트레이드오프 요약은 `docs/performance-report.md`, `docs/tradeoff-summary.md`처럼 knowledge와 분리된 최상위 문서로 둔다 (지식 정리 vs 실측 결과 구분).

---

## 5. 부하/성능 테스트 디렉토리

```
load-test/{tool}/
├── ScenarioASimulation.scala   정합성 검증용 극한 경합 시나리오
├── ScenarioBSimulation.scala   실사용 플로우 기반 처리량 시나리오
```

- 최소 두 종류의 시나리오를 구분해서 설계한다: **(a) 정합성 검증**(적은 재고 + 높은 동시성으로 오버부킹/중복 등 버그를 드러냄) / **(b) 처리량 측정**(실제 사용자 플로우 + think time으로 TPS 측정).
- 테스트 전 상태 초기화 API(`/reset`)를 반드시 `@Profile("!prod")` 등으로 프로덕션에서 비활성화한다.
- 결과는 코드가 아니라 DB/대시보드에 저장해서 버전 간 비교가 가능하게 한다.

---

## 6. CLAUDE.md(또는 AGENTS.md) 안에 반드시 넣을 섹션

새 프로젝트에서 지침서를 쓸 때 아래 섹션 구성을 그대로 가져간다:

1. **프로젝트 개요** — 목표, 타겟, 핵심 어필 포인트 (포트폴리오성 프로젝트라면 "왜 이 기술을 선택했는지"까지)
2. **기술 스택 표**
3. **프로젝트 구조** (트리 형태, 실제 디렉토리와 동기화 유지)
4. **버전별/전략별 구현 설명** — 각 버전이 무엇을 다르게 하는지 1~3줄 요약
5. **함정 & 수정 이력 표** — `#번호 | 버전 | 함정 | 파일 | 수정 방향` 형식. 코드 리뷰나 프레젠테이션 후 발견한 문제를 계속 누적한다.
6. **Phase 완료 체크리스트** — `[x]`/`[ ]`로 진행 상황을 CLAUDE.md 안에서 직접 추적한다. 세부 하위 작업은 `8-1`, `8-2`처럼 번호를 매겨 나열.
7. **코딩 컨벤션 & 규칙** — 아래 7번 참고.

---

## 7. 코딩 컨벤션 섹션에 포함할 원칙 (일반화)

```
가독성 원칙
- 메서드는 한 가지 일만 한다 (조회+검증+저장 동시 금지, 20줄 초과 시 분리)
- 메서드/변수 이름은 동작·의미를 그대로 설명한다
- 매직 넘버는 상수로 추출한다
- 조건문은 의미 있는 이름의 메서드로 추출한다 (validateX, isY)

복잡도 원칙
- 인터페이스는 구현체 2개 이상 또는 테스트 대역 필요 시에만
- 불필요한 레이어(XxxImpl 패턴 등) 금지
- 커스텀 예외로 의미를 부여 (예: SoldOutException)
- Lombok 등 어노테이션 사용 범위를 명시적으로 제한 (예: @Getter만 허용, @Data/@Setter 금지)

로그 원칙
- 상태 전이/락 이벤트 등 핵심 이벤트는 INFO로 남겨 이후 분석에 활용

구조 원칙
- 컨트롤러는 라우팅만, 비즈니스 로직 없음
- 예외는 전역 핸들러(@RestControllerAdvice 등)에서 통일 처리
- 위험한 운영 API(초기화 등)는 프로파일로 분리

코드 작성 단위 원칙 (AI 협업 시 특히 중요)
- 한 번에 하나의 파일만 작성
- 논리적 단계가 2개 이상이면 나눠서 진행 (엔티티 → 확인 → 레포지토리 → 확인 → 서비스)
- 각 단계 완료 시 확인 요청 멘트를 남기고, 응답을 받은 후에만 다음 단계 진행
```

---

## 이 프로젝트에 적용된 실제 예시

구체적인 이름이 궁금하면 이 저장소의 `CLAUDE.md`를 참고한다 (도메인: `concert`, `reservation`(v1~v6), `waitingroom`, `payment`, `dashboard`, `global`). 다른 프로젝트에 옮길 때는 도메인 이름과 버전 개수만 그 프로젝트 맥락에 맞게 바꾸면 된다.
